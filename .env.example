# medAgent-CRM environment variables.
# Copy to .env and fill in real values. New specs add to this file as they
# introduce new env vars (see steering/02-code-quality.md).
#
# No env vars are required yet at this stage (MEDGENT-001) — neither app
# reads any configuration. Postgres, Groq, and API base URL vars land with
# MEDGENT-002/003/005/009/017.
